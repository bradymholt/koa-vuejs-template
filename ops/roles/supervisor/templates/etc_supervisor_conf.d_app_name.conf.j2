[program:{{app_name}}]
command={{ start_command }}
directory={{deploy_directory}}
autostart=true
autorestart=true
stderr_logfile={{ error_file }}
stdout_logfile={{ log_file }}
environment=
user={{webserver_user}}
stopsignal=INT
stopasgroup=true
killasgroup=true
